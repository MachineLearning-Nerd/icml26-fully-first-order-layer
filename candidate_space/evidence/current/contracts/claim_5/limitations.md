# Claim 5 limitations and deviations

“Minimal code changes” is an interface claim rather than a numerical theorem.
The test demonstrates the same DPP problem, parameter list, variable list,
positional tensors, tuple output, and `solver_args` call shape. FFOLayer adds
method-specific tolerances; it is not asserted to be a character-for-character
constructor replacement.

