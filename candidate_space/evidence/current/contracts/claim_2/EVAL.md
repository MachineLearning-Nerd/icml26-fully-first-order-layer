# Claim 2 evaluation

Run `uv run --frozen python -m reproduction.run`. The verifier writes raw JSON and exits nonzero on any contract failure. `reproduction/check.py` independently checks thresholds and row counts.

