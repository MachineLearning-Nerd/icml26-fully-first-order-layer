# Claim 2 limitations and deviations

The exact theorem is local. The deterministic instances deliberately remain away from active-set transitions. The boundary control demonstrates why the excluded transition case cannot be silently treated as positive evidence.

